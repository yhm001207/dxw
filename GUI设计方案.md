# DXW 桌面备份客户端 - GUI 重设计方案

> 基于现有 sync_client.py tkinter 应用，重构为现代桌面端 UI

---

## 一、技术栈建议

| 方案 | 推荐指数 | 理由 |
|------|----------|------|
| **PySide6 (Qt)** | ⭐⭐⭐⭐⭐ | Python 原生，组件完整，主题系统成熟，系统托盘原生支持，跨平台 |
| **Electron + React** | ⭐⭐⭐⭐ | Web 技术栈，UI 表现力最强，但打包体积大 (~150MB) |
| **Tauri + React** | ⭐⭐⭐⭐ | Rust 后端，打包小 (~5MB)，但需要 Rust 编译环境 |
| **CustomTkinter** | ⭐⭐⭐ | 对现有代码改动最小，但组件库有限，上限低 |

**推荐：PySide6** - 与现有 Python 代码无缝集成，Qt 的 QSS (Qt Style Sheets) 可实现精致 UI，QSystemTrayIcon 原生托盘，QTreeView/QTableView 性能优秀。

---

## 二、产品整体结构

```
┌──────────────────────────────────────────────────────────────┐
│  DXW 备份客户端 v1.0                              _ □ ×   │
├──────────┬───────────────────────────────────────────────────┤
│          │  🔵 已连接  ▲ 2.3 MB/s  ⏳ 正在备份(3)  💾 45GB剩余│
│  🏠 首页  ├───────────────────────────────────────────────────┤
│  📦 任务  │                                                   │
│  ☁️ 云端  │              [中央内容区]                         │
│  📋 日志  │                                                   │
│  ⚙️ 设置  │                                                   │
│          │                                                   │
│          │                                                   │
│          │                                                   │
│          ├───────────────────────────────────────────────────┤
│          │  v1.0.0  |  🔼 最小化到托盘  |  ⏸ 暂停同步       │
├──────────┴───────────────────────────────────────────────────┤
│  💡 状态栏: 上次同步: 2秒前 | 文件数: 1,234 | 已完成: 89%   │
└──────────────────────────────────────────────────────────────┘
```

### 窗口层级

```
App (QMainWindow)
├── TitleBar (自定义标题栏，非系统原生)
├── Sidebar (左侧导航)
│   ├── NavItem: 首页
│   ├── NavItem: 备份任务
│   ├── NavItem: 文件管理
│   ├── NavItem: 日志
│   └── NavItem: 设置
├── TopBar (顶部状态栏)
│   ├── ConnectionStatus (连接状态指示器)
│   ├── TransferSpeed (实时传输速度)
│   ├── CurrentTask (当前任务信息)
│   └── StorageQuota (剩余空间)
├── ContentArea (QStackedWidget)
│   ├── DashboardPage (首页概览)
│   ├── TasksPage (备份任务)
│   ├── FilesPage (文件管理)
│   ├── LogsPage (日志)
│   └── SettingsPage (设置)
├── BottomBar (底部操作栏)
│   ├── VersionLabel (版本号)
│   ├── MinimizeToTrayBtn (最小化到托盘)
│   └── QuickActions (快速操作)
└── StatusBar (状态栏)
    ├── LastSyncTime (上次同步时间)
    ├── FileCount (文件数统计)
    └── Progress (总体进度)
```

### 二级弹窗 / 对话框

```
Dialogs
├── SettingsDialog (设置弹窗 - 从设置页可触发)
│   ├── ServerConfigTab (服务器配置)
│   ├── AccountTab (账号登录)
│   ├── ScheduleTab (定时策略)
│   ├── NetworkTab (带宽/代理)
│   └── NotifyTab (通知)
├── AddTaskWizard (添加任务向导 - 从任务页触发)
│   ├── Step1: SelectFolder (选择本地文件夹)
│   ├── Step2: CloudPath (选择云端路径)
│   ├── Step3: BackupStrategy (备份策略设置)
│   └── Step4: Confirm (确认完成)
├── FilePreviewDialog (文件预览 - 从文件管理触发)
├── LogDetailDialog (日志详情 - 从日志页触发)
├── ConfirmDialog (确认对话框)
└── AboutDialog (关于)
```

---

## 三、UI 风格规范

### 3.1 配色方案

#### 浅色模式

| 用途 | 色值 | 变量名 |
|------|------|--------|
| 主色调 | `#4F6EF7` | `--primary` |
| 主色悬停 | `#3B56D9` | `--primary-hover` |
| 主色浅底 | `#EEF1FF` | `--primary-bg` |
| 背景 | `#F5F6FA` | `--bg` |
| 卡片背景 | `#FFFFFF` | `--card-bg` |
| 侧栏背景 | `#FFFFFF` | `--sidebar-bg` |
| 顶部栏背景 | `#FFFFFF` | `--topbar-bg` |
| 文字主色 | `#1A1D26` | `--text-primary` |
| 文字次要 | `#6B7280` | `--text-secondary` |
| 文字浅色 | `#9CA3AF` | `--text-muted` |
| 边框 | `#E5E7EB` | `--border` |
| 分割线 | `#F0F0F5` | `--divider` |
| 成功 | `#10B981` | `--success` |
| 警告 | `#F59E0B` | `--warning` |
| 错误 | `#EF4444` | `--error` |
| 信息 | `#3B82F6` | `--info` |

#### 深色模式

| 用途 | 色值 | 变量名 |
|------|------|--------|
| 主色调 | `#6B83FF` | `--primary` |
| 主色悬停 | `#8599FF` | `--primary-hover` |
| 主色浅底 | `#252A4A` | `--primary-bg` |
| 背景 | `#14161F` | `--bg` |
| 卡片背景 | `#1C1E2E` | `--card-bg` |
| 侧栏背景 | `#191B28` | `--sidebar-bg` |
| 顶部栏背景 | `#1C1E2E` | `--topbar-bg` |
| 文字主色 | `#E8EAF0` | `--text-primary` |
| 文字次要 | `#9498A8` | `--text-secondary` |
| 文字浅色 | `#5C6070` | `--text-muted` |
| 边框 | `#2A2D3E` | `--border` |
| 分割线 | `#222436` | `--divider` |
| 成功 | `#34D399` | `--success` |
| 警告 | `#FBBF24` | `--warning` |
| 错误 | `#F87171` | `--error` |
| 信息 | `#60A5FA` | `--info` |

### 3.2 字体规范

| 层级 | 字号 | 字重 | 行高 |
|------|------|------|------|
| 页面标题 | 20px | Bold (700) | 28px |
| 卡片标题 | 15px | Semibold (600) | 22px |
| 正文 | 13px | Regular (400) | 20px |
| 辅助文字 | 12px | Regular (400) | 18px |
| 小标签 | 11px | Medium (500) | 16px |
| 数字/统计 | 28px | Bold (700) | 36px |

字体栈：`"Segoe UI Variable", "Segoe UI", -apple-system, "Microsoft YaHei", sans-serif`

### 3.3 间距体系

| 层级 | 值 | 使用场景 |
|------|-----|----------|
| XS | 4px | 图标与文字间距 |
| SM | 8px | 卡片内元素间距 |
| MD | 12px | 组件内部间距 |
| LG | 16px | 卡片内边距 |
| XL | 20px | 组件间距 |
| XXL | 24px | 区域间距 |
| XXXL | 32px | 页面边距 |

### 3.4 圆角规范

| 层级 | 值 | 使用场景 |
|------|------|----------|
| SM | 4px | 输入框、小按钮 |
| MD | 8px | 卡片、面板 |
| LG | 12px | 模态弹窗 |
| XL | 16px | 大卡片、容器 |
| FULL | 50% | 头像、状态圆点 |

### 3.5 阴影规范

| 层级 | 浅色模式 | 深色模式 | 使用场景 |
|------|----------|----------|----------|
| SM | `0 1px 2px rgba(0,0,0,0.05)` | `0 1px 2px rgba(0,0,0,0.2)` | 卡片默认 |
| MD | `0 4px 12px rgba(0,0,0,0.08)` | `0 4px 12px rgba(0,0,0,0.3)` | 卡片悬停 |
| LG | `0 8px 24px rgba(0,0,0,0.12)` | `0 8px 24px rgba(0,0,0,0.4)` | 下拉菜单、弹窗 |

### 3.6 组件规范

#### 按钮

```
主按钮 (Primary)
┌────────────────────┐
│  Normal  │ Hover   │ Active  │ Disabled │
│  #4F6EF7  │ #3B56D9 │ #2E45C0 │ #B5C0F5 │
│  白字      │ 白字     │ 白字     │ 白字     │
└────────────────────┘

次要按钮 (Secondary)
┌────────────────────┐
│  Normal    │ Hover     │ Active    │ Disabled   │
│  透明边框    │ 浅色背景   │ 深色背景   │ 浅色文字   │
└────────────────────┘

文字按钮 (Text)
┌────────────────────┐
│  Normal  │ Hover     │ Active     │ Disabled  │
│  无色背景  │ 浅灰背景   │ 灰色背景   │ 无色背景   │
└────────────────────┘

尺寸:
  Large:  height 40px, padding 0 20px, font 14px
  Medium: height 34px, padding 0 16px, font 13px  (默认)
  Small:  height 28px, padding 0 12px, font 12px
```

#### 输入框

```
┌─────────────────────────────────────────────┐
│  标签文字                             12px  │
│  ┌───────────────────────────────────────┐  │
│  │ placeholder text          Label       │  │
│  │ #1A1D26                   #9CA3AF    │  │
│  │ border: #E5E7EB           focus:     │  │
│  │                           2px #4F6EF7│  │
│  └───────────────────────────────────────┘  │
│  ⚠️ 错误提示文字 (错误态时显示)     #EF4444  │
└─────────────────────────────────────────────┘
```

#### 卡片

```
┌──────────────────────────────────────────────┐
│  bg: #FFFFFF / #1C1E2E (深色)                │
│  border-radius: 8px                          │
│  padding: 16px                               │
│  box-shadow: 0 1px 2px rgba(0,0,0,0.05)      │
│                                              │
│  ┌─────┐                                     │
│  │ 图标 │  标题                    ↗ 更多     │
│  └─────┘                                     │
│  内容区域                                     │
└──────────────────────────────────────────────┘
```

#### 进度条

```
▰▰▰▰▰▰▰▰▰▰▰▰▰▰▰▰▰▰▰▱▱▱▱▱▱  72% (45MB/63MB)
  已完成: #4F6EF7  |  进行中: #B5C0F5
  失败段: #FCA5A5 (如有失败)
  高度: 6px，圆角: 3px
```

#### 表格/列表

```
┌──────┬────────────┬──────────┬──────────┬─────────┐
│ 状态 │ 文件名      │ 大小     │ 修改时间  │ 操作    │
├──────┼────────────┼──────────┼──────────┼─────────┤
│ ✅   │ report.pdf │ 2.3 MB   │ 05-14    │ [下载]  │
│ ⏳   │ data.zip   │ 45 MB    │ 05-13    │ [下载]  │
│ ❌   │ old.docx   │ 1.1 MB   │ 05-12    │ [下载]  │
└──────┴────────────┴──────────┴──────────┴─────────┘

行高: 44px
悬停: 背景色变化 (#F5F6FA / #1E2030 深色)
选中: 主色浅底 (#EEF1FF / #252A4A 深色)
交替行: 可选
```

---

## 四、每个页面的详细布局

### 4.1 首页 - DashboardPage

```
┌─────────────────────────────────────────────────────────────┐
│  🏠 首页                                                    │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  📊 备份概览   │  │  📦 文件统计  │  │  ⚡ 传输状态  │      │
│  │               │  │              │  │              │      │
│  │  24           │  │  1,234 文件  │  │  ▲ 2.3 MB/s  │      │
│  │  次成功备份    │  │  45.2 GB     │  │  上传中       │      │
│  │  2 次失败      │  │  3 个任务    │  │  0 B/s       │      │
│  │               │  │              │  │  空闲         │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  📋 最近活动                              [查看全部] │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │  ✅ 14:23:45  report.pdf → 云端备份 完成  2.3 MB    │   │
│  │  ⏳ 14:23:40  data.zip → 云端备份中   12%  /uploads │   │
│  │  ❌ 14:20:00  old.docx → 云端同步失败  网络超时    │   │
│  │  ✅ 14:15:00  config.json → 云端备份完成  12 KB   │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  快速操作                                             │   │
│  │                                                      │   │
│  │  [📁 立即同步]  [➕ 新建任务]  [📂 打开本地目录]  [⚙️ 设置] │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 备份概览卡片：显示今日成功/失败次数、总备份数据量
- 文件统计卡片：总文件数、总大小、备份任务数
- 传输状态卡片：实时上传/下载速度，当前状态指示
- 最近活动列表：展示最近的同步操作记录（最多 10 条），每条包含时间、文件名、状态、大小
- 快速操作栏：一键触发核心操作

**状态**：
- 空状态：初次使用，无备份记录 → 显示引导提示 + "创建第一个备份任务"按钮
- 加载中：骨架屏占位
- 错误状态：连接失败 → 提示 + 重试按钮

---

### 4.2 备份任务页 - TasksPage

```
┌─────────────────────────────────────────────────────────────┐
│  📦 备份任务                            [+ 添加任务]        │
│                                                             │
│  ┌──────────┬───────────────────────────────────────────┐   │
│  │ 筛选/搜索│  [全部] [进行中] [已完成] [失败]  🔍 搜索  │   │
│  ├──────────┴───────────────────────────────────────────┤   │
│  │                                                      │   │
│  │  ┌──────────────────────────────────────────────┐    │   │
│  │  │  📁 我的文档                                  │    │   │
│  │  │  C:\Users\xxx\Documents  →  /backup/docs      │    │   │
│  │  │  ▰▰▰▰▰▰▰▰▱▱▱▱▱▱▱  38%  |  ⏸ 暂停  |  ⋮    │    │   │
│  │  │  45/120 个文件 | 12.3 MB / 45.2 MB           │    │   │
│  │  │  策略: 实时同步 | 增量备份 | 版本保留: 7天   │    │   │
│  │  └──────────────────────────────────────────────┘    │   │
│  │                                                      │   │
│  │  ┌──────────────────────────────────────────────┐    │   │
│  │  │  📁 项目代码                                  │    │   │
│  │  │  D:\Projects  →  /backup/projects             │    │   │
│  │  │  ✅ 已完成  |  2026-05-14 14:00              │    │   │
│  │  │  3,412 个文件 | 1.2 GB                        │    │   │
│  │  │  策略: 每日 02:00 | 全量备份 | 版本保留: 30天│    │   │
│  │  │  [📥 立即同步] [✏️ 编辑] [🗑️ 删除]           │    │   │
│  │  └──────────────────────────────────────────────┘    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 每个备份任务以独立卡片展示，包含完整信息
- 进度条实时展示当前任务进度
- 筛选/搜索功能，支持按状态过滤
- 卡片操作：暂停/恢复、立即同步、编辑、删除
- 添加任务按钮 → 弹出添加任务向导

**添加任务向导 (AddTaskWizard)**：

```
Step 1: 选择本地文件夹
┌────────────────────────────────────────────┐
│  添加备份任务              Step 1/4        │
│                                            │
│  选择要备份的本地文件夹                      │
│  ┌──────────────────────────────────┐      │
│  │ C:\Users\xxx\Documents     [浏览]│      │
│  └──────────────────────────────────┘      │
│  或拖拽文件夹到此处 📂                    │
│                                            │
│  ┌────────────────────┐                   │
│  │ 下一步  →          │                   │
│  └────────────────────┘                   │
└────────────────────────────────────────────┘

Step 2: 选择云端路径
┌────────────────────────────────────────────┐
│  添加备份任务              Step 2/4        │
│                                            │
│  选择云端目标文件夹                        │
│  ┌──────────────────────────────────┐      │
│  │  我的云盘 > backup > docs        │      │
│  │  ├── projects                    │      │
│  │  └── docs            [新建文件夹] │      │
│  └──────────────────────────────────┘      │
│                                            │
│  ┌──────────────┐  ┌──────────────┐       │
│  │  ← 上一步    │  │  下一步 →    │       │
│  └──────────────┘  └──────────────┘       │
└────────────────────────────────────────────┘

Step 3: 备份策略设置
┌────────────────────────────────────────────┐
│  添加备份任务              Step 3/4        │
│                                            │
│  备份频率:                                 │
│  ○ 实时同步 (文件变更后立即备份)           │
│  ● 定时备份                               │
│     ┌─────────────────┐ 每天  02:00       │
│     └─────────────────┘                   │
│  ○ 手动备份                               │
│                                            │
│  备份方式:  [● 增量备份] [○ 全量备份]      │
│  版本保留:  [30]  天                       │
│  冲突策略:  [本地文件优先 ▼]               │
│                                            │
│  ┌──────────────┐  ┌──────────────┐       │
│  │  ← 上一步    │  │  完成创建    │       │
│  └──────────────┘  └──────────────┘       │
└────────────────────────────────────────────┘
```

**状态**：
- 空状态：无任何任务 → 显示引导插图 + "立即添加第一个备份"按钮
- 加载中：骨架屏
- 错误状态：连接断开 → 提示重连

---

### 4.3 文件管理页 - FilesPage

```
┌─────────────────────────────────────────────────────────────┐
│  ☁️ 云端文件管理                                             │
│                                                             │
│  当前路径: 我的云盘 / backup / documents                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  📁 .. (上级目录)                                     │   │
│  │  📁 images                    2026-05-14   -         │   │
│  │  📄 report.pdf    2.3 MB     2026-05-14   [下载][🗑️] │   │
│  │  📄 data.zip      45.0 MB    2026-05-13   [下载][🗑️] │   │
│  │  📄 config.json   12 KB      2026-05-12   [下载][🗑️] │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  选中 2 项  |  总计 47.3 MB  |                            │
│  [⬇ 下载选中] [🗑 删除选中] [📂 新建文件夹] [🔄 刷新]      │
└─────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 面包屑导航：显示当前云端路径，可点击跳转
- 文件列表：文件名、大小、修改时间、操作按钮
- 多选操作：支持 Shift/Ctrl 多选，批量下载和删除
- 操作按钮：下载、删除、新建文件夹、刷新、上传
- 右键菜单：下载、复制路径、重命名、删除
- 拖拽上传：支持拖拽文件/文件夹到列表区上传

**状态**：
- 空状态：文件夹为空 → 显示"此文件夹为空" + "上传文件"按钮
- 加载中：骨架屏或加载动画
- 错误状态：网络错误 → 重试按钮

---

### 4.4 日志页 - LogsPage

```
┌─────────────────────────────────────────────────────────────┐
│  📋 同步日志                                                │
│                                                             │
│  [📥 导出日志] [🗑 清空日志]                                 │
│                                                             │
│  🔍 搜索  |  [全部] [成功] [失败] [警告]                     │
│                                                             │
│  ┌──────┬──────────┬────────────┬──────────┬──────────┐     │
│  │ 时间  │ 类型     │ 文件        │ 大小     │ 详情     │     │
│  ├──────┼──────────┼────────────┼──────────┼──────────┤     │
│  │ 14:23 │ ✅ 上传  │ report.pdf │ 2.3 MB  │ 完成     │     │
│  │ 14:23 │ ⏳ 上传  │ data.zip   │ 45 MB   │ 12%      │     │
│  │ 14:20 │ ❌ 上传  │ old.docx   │ 1.1 MB  │ 网络超时  │     │
│  │ 14:15 │ ✅ 下载  │ config.js  │ 12 KB   │ 完成     │     │
│  ├──────┼──────────┼────────────┼──────────┼──────────┤     │
│  │   共 1,234 条                                 1/247页 │   │
│  └──────┴──────────┴────────────┴──────────┴──────────┘     │
└─────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 表格展示日志：时间、类型（成功/失败/警告 带图标）、文件名、大小、详情
- 筛选过滤：搜索文件名、按状态筛选
- 分页：每页 50 条，支持页码导航
- 导出：导出为 CSV/TXT 文件
- 清空：清空所有日志（确认弹窗）
- 点击详情行 → 展开详情或弹出详情弹窗

**状态**：
- 空状态：暂无日志 → 显示"还没有同步记录"
- 加载中：表格骨架屏

---

### 4.5 设置页 - SettingsPage

```
┌─────────────────────────────────────────────────────────────┐
│  ⚙️ 设置                                                    │
│                                                             │
│  ┌── 服务器 ─────────────────────────────────────────────┐  │
│  │  服务器地址  ┌────────────────────────────┐           │  │
│  │             │ http://192.168.1.100       │           │  │
│  │             └────────────────────────────┘           │  │
│  │  端口号      ┌──────┐                                │  │
│  │             │ 5000 │                                │  │
│  │             └──────┘                                │  │
│  │  用户名      ┌────────────────────────────┐           │  │
│  │             │ username                    │           │  │
│  │             └────────────────────────────┘           │  │
│  │  密码        ┌────────────────────────────┐           │  │
│  │             │ •••••••••                  │           │  │
│  │             └────────────────────────────┘           │  │
│  │  [🧪 测试连接]  ✅ 连接成功 (延迟 12ms)              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌── 备份设置 ────────────────────────────────────────────┐ │
│  │  定时备份  [● 启用] [○ 禁用]                            │ │
│  │  备份间隔  [60 ▼]  分钟                                │ │
│  │  备份方式  [● 增量备份] [○ 全量备份]                    │ │
│  │  冲突处理  [保留较新的版本 ▼]                           │ │
│  │  失败重试  3  次 (每次间隔 30 秒)                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌── 网络 ────────────────────────────────────────────────┐ │
│  │  上传限速  [● 不限速] [○ 限制]  ┌──────┐ MB/s       │ │
│  │  下载限速  [● 不限速] [○ 限制]  ┌──────┐ MB/s       │ │
│  │  代理设置  ┌────────────────────────────┐              │ │
│  │           │ (可选) HTTP 代理地址       │              │ │
│  │           └────────────────────────────┘              │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌── 通用 ────────────────────────────────────────────────┐ │
│  │  [✓] 开机自启                                          │ │
│  │  [✓] 最小化到托盘                                      │ │
│  │  [✓] 同步完成通知                                      │ │
│  │  [✓] 同步失败通知                                      │ │
│  │  [✓] 深色模式                                          │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  [💾 保存设置]                                       │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**功能说明**：
- 分区块设置：服务器、备份、网络、通用
- 测试连接：带实时反馈（成功/失败 + 延迟）
- 设置即时生效或保存后生效（使用保存按钮）
- 深色模式切换即时预览

---

## 五、交互流程

### 5.1 主流程

```
启动应用
  │
  ├── 首次启动? ──是──→ 配置向导 (服务器地址 → 账号 → 测试连接 → 添加备份目录)
  │
  └── 非首次启动
        │
        ├── 读取本地配置
        ├── 自动尝试连接服务器
        │     ├── 连接成功 → 进入首页
        │     └── 连接失败 → 首页显示错误提示 + 引导到设置页
        │
        ├── 自动开始定时同步
        │
        └── 用户操作循环
              ├── 首页: 查看状态、手动同步
              ├── 任务页: 管理备份任务
              ├── 文件页: 管理云端文件
              ├── 日志页: 查看历史记录
              └── 设置页: 修改配置
```

### 5.2 状态机

```
                     ┌──────────┐
                     │  DISCONN  │ ←─── 网络断开/未配置
                     │  未连接   │
                     └────┬─────┘
                          │ 配置服务器 + 连接成功
                          ▼
                     ┌──────────┐
            ┌──────→ │  IDLE    │ ←──── 所有任务完成
            │        │  空闲    │
            │        └────┬─────┘
            │             │ 定时触发 / 手动触发 / 文件变更
            │        ┌────▼─────┐
            │        │  SYNCING │ ←──── 正在同步
            │        │  同步中   │
            │        └────┬─────┘
            │             │ 任务完成
            │        ┌────▼─────┐
            │        │  PAUSED  │ ←──── 用户暂停
            │        │  已暂停   │
            │        └────┬─────┘
            │             │ 用户恢复
            └─────────────┘

  异常状态:
  ┌──────────┐    ┌──────────┐    ┌──────────┐
  │  ERROR   │    │  RETRY   │    │  RATE    │
  │  错误    │    │  重试中   │    │  限速    │
  │  连接失败 │    │  第3/3次  │    │  带宽受限 │
  └──────────┘    └──────────┘    └──────────┘
```

### 5.3 托盘交互

```
系统托盘图标 (状态指示):

  🟢 绿色: 正常运行 / 空闲
  🔵 蓝色: 同步中
  🟡 黄色: 警告 (部分失败)
  🔴 红色: 错误 (连接断开)

右键菜单:
  ┌─────────────────┐
  │  📂 打开主窗口   │  → showWindow()
  │  ─────────────── │
  │  ▶ 立即同步     │  → triggerSync()
  │  ⏸ 暂停同步     │  → togglePause()
  │  ─────────────── │
  │  ⚙️ 设置        │  → openSettings()
  │  📋 日志        │  → openLogs()
  │  ─────────────── │
  │  ✕ 退出         │  → quitApp()
  └─────────────────┘
```

---

## 六、功能优化清单

### 新增功能

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| F-01 | **多任务管理** | 支持同时管理多个备份任务（不同目录、不同策略），卡片式展示 | P0 |
| F-02 | **定时策略配置** | 实时/按小时/每日/每周/自定义 cron 表达式 | P0 |
| F-03 | **带宽限速** | 上传/下载独立限速，支持不限速/限制速度 | P0 |
| F-04 | **版本保留策略** | 按天数保留历史版本，自动清理过期备份 | P0 |
| F-05 | **开机自启** | 注册到 Windows 启动项，支持开机自动最小化启动 | P0 |
| F-06 | **深色/浅色模式** | 跟随系统或手动切换 | P0 |
| F-07 | **连接测试** | 配置服务器后一键测试连接，显示延迟和可用空间 | P0 |
| F-08 | **配置向导** | 首次启动引导式配置流程 | P1 |
| F-09 | **日志导出** | 导出为 CSV/TXT 格式 | P1 |
| F-10 | **网络中断自动重连** | 检测到连接恢复后自动继续同步 | P1 |
| F-11 | **文件冲突对比** | 冲突时显示本地/云端版本信息，提供解决选项 | P2 |
| F-12 | **备份计划统计** | 展示备份成功率、趋势图表 | P2 |
| F-13 | **文件搜索** | 云端文件按名称搜索 | P2 |
| F-14 | **选择性同步** | 配置排除规则（排除某类文件/文件夹） | P2 |
| F-15 | **代理支持** | HTTP/HTTPS 代理配置 | P2 |

### 改进功能

| 编号 | 功能 | 改进内容 | 优先级 |
|------|------|----------|--------|
| I-01 | **同步引擎** | 迁移到 PySide6 QThread，替代 threading + queue 模式 | P0 |
| I-02 | **配置存储** | JSON → SQLite 或加密存储（密码加密保存） | P1 |
| I-03 | **上传体验** | 实时进度反馈，取消上传，断点续传 | P1 |
| I-04 | **内存优化** | 保持现有分片上传策略，增加大文件预览限制 | P2 |
| I-05 | **通知系统** | Windows 原生 Toast 通知，替代 print/log | P1 |

### 体验提升

| 编号 | 改进 | 说明 | 优先级 |
|------|------|------|--------|
| E-01 | **空状态设计** | 每个页面空状态均有引导提示和操作入口 | P0 |
| E-02 | **加载状态** | 骨架屏/加载动画，避免白屏 | P0 |
| E-03 | **错误处理** | 统一错误展示，提供具体错误信息和解决建议 | P0 |
| E-04 | **过渡动画** | 页面切换、卡片展开收起等流畅动画 | P1 |
| E-05 | **键盘快捷键** | 支持快捷键操作（Ctrl+S 保存、Ctrl+F 搜索等） | P2 |
| E-06 | **拖拽交互** | 拖拽添加文件夹、拖拽上传文件 | P1 |
| E-07 | **批量操作** | 多选后批量下载/删除 | P1 |

---

## 七、前端页面结构与模块命名

### 目录结构 (PySide6)

```
sync_client/
├── main.py                          # 应用入口
├── app.py                           # QApplication 初始化 + 主题管理
├── config.py                        # 配置读写 (加密存储)
├── requirements.txt
│
├── core/
│   ├── __init__.py
│   ├── sync_engine.py               # 同步引擎 (QThread)
│   ├── sync_worker.py               # 同步工作线程
│   ├── file_watcher.py              # 文件监控
│   ├── chunked_uploader.py          # 分片上传
│   ├── cloud_api.py                 # 云端 API 封装
│   └── db.py                        # SQLite 数据库操作
│
├── ui/
│   ├── __init__.py
│   ├── theme.py                     # 主题/配色/样式常量
│   ├── main_window.py               # 主窗口 (QMainWindow)
│   ├── widgets/
│   │   ├── __init__.py
│   │   ├── title_bar.py             # 自定义标题栏
│   │   ├── sidebar.py               # 左侧导航栏
│   │   ├── top_bar.py               # 顶部状态栏
│   │   ├── bottom_bar.py            # 底部操作栏
│   │   ├── status_bar.py            # 状态栏
│   │   ├── task_card.py             # 任务卡片组件
│   │   ├── file_table.py            # 文件表格组件
│   │   ├── log_table.py             # 日志表格组件
│   │   ├── progress_bar.py          # 进度条组件
│   │   ├── connection_indicator.py  # 连接状态指示灯
│   │   └── toast.py                 # Toast 消息提示
│   │
│   └── pages/
│       ├── __init__.py
│       ├── dashboard_page.py        # 首页
│       ├── tasks_page.py            # 备份任务页
│       ├── files_page.py            # 文件管理页
│       ├── logs_page.py             # 日志页
│       └── settings_page.py         # 设置页
│
├── dialogs/
│   ├── __init__.py
│   ├── add_task_wizard.py           # 添加任务向导 (4步)
│   ├── settings_dialog.py           # 设置弹窗
│   ├── confirm_dialog.py            # 确认对话框
│   ├── about_dialog.py              # 关于
│   └── file_preview_dialog.py       # 文件预览
│
├── resources/
│   ├── icons/                       # SVG 图标
│   ├── styles/
│   │   ├── light.qss                # 浅色主题样式表
│   │   └── dark.qss                 # 深色主题样式表
│   └── images/                      # 插图 (空状态等)
│
└── tests/
    ├── test_sync_engine.py
    ├── test_cloud_api.py
    └── test_db.py
```

### 关键类设计

```python
# main_window.py
class MainWindow(QMainWindow):
    def __init__(self):
        self.sidebar = Sidebar(self)
        self.top_bar = TopBar(self)
        self.bottom_bar = BottomBar(self)
        self.status_bar = StatusBar(self)
        self.content_stack = QStackedWidget(self)
        # pages
        self.dashboard = DashboardPage(self)
        self.tasks = TasksPage(self)
        self.files = FilesPage(self)
        self.logs = LogsPage(self)
        self.settings = SettingsPage(self)
        # engine
        self.engine = SyncEngine(self)
        self.watcher = FileWatcher(self)

# sync_engine.py
class SyncEngine(QObject):
    sync_started = Signal()
    sync_progress = Signal(int, int, str)  # current, total, filename
    sync_completed = Signal(dict)          # result stats
    sync_error = Signal(str)               # error message
    status_changed = Signal(str)           # idle/syncing/paused/error

    def start_loop(self):
    def stop_loop(self):
    def pause(self):
    def resume(self):
    def sync_now(self):

# theme.py
class Theme:
    LIGHT = { ... }
    DARK = { ... }
    @staticmethod
    def apply(app, mode):
    @staticmethod
    def get_style(mode):
```

---

## 八、实施建议

1. **渐进式重构**：保留现有 `sync_client.py` 的同步引擎逻辑，先用 PySide6 封装 UI，后逐步重构引擎层
2. **先核心后周边**：第一版只做 首页 + 任务 + 设置 三个核心页面，文件和日志后续迭代
3. **QSS 主题分离**：样式全部写在 `.qss` 文件中，和逻辑分离，便于后期换肤
4. **信号槽解耦**：引擎和 UI 完全通过 Qt Signal/Slot 通信，不直接调用 UI 方法
5. **打包**：使用 PyInstaller + `--windowed` 模式打包为单 EXE
