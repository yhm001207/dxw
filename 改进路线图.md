# 远程服务器控制面板 — 改进路线图

## 已完成

- [x] **性能监控** — CPU 名称/温度/使用率、内存、GPU 监控卡片
- [x] **多卡并行** — DataParallel 自动包装开关
- [x] **WebSocket 终端** — 真实 cmd/PowerShell/Git Bash PTY 终端，支持 xterm.js
- [x] **文件夹上传** — 按钮 + 拖拽上传整个文件夹
- [x] **批量上传修复** — 多文件上传不再只传第一个
- [x] **图片标签页预览** — 图片直接在编辑器标签页中打开
- [x] **管理员启动脚本** — start_admin.bat / stop_server.bat / restart_server.bat
- [x] **隐藏启动** — pythonw 无窗口运行
- [x] **左侧面板优化** — 单击打开文件、复选框选中、高亮显示

---

## 待实现

### 一、用户设置持久化

**问题**: 常用文件夹设置存在浏览器 localStorage，换浏览器/清缓存就丢失

**方案**:
- 后端新增 `GET/POST /api/user_settings`，用 `users/<username>/settings.json` 存储
- 前端 `toggleFavorite()` / `clearFavorite()` / `updateFavBtnState()` 改用服务端 API
- 页面初始化先请求服务端获取 `favorite_path`

---

### 二、个人云盘 UI 完善

**问题**: 后端 API 齐全但前端 UI 缺失

**改进项**:
- 工具栏：上传按钮、新建文件夹、批量删除、批量下载（zip）、搜索框
- 文件列表：每行 checkbox 多选、全选、选中高亮
- 右键菜单：预览/下载/重命名/移动/删除
- 拖拽上传进度条
- 文件类型图标细化（.py .txt .csv .pdf 等）

**后端新增**:
- `POST /api/batch_delete` — 批量删除
- `POST /api/batch_move` — 批量移动
- `POST /api/batch_download` — 打包 zip 下载

---

### 三、通知系统

**场景**: 训练完成、任务出错、文件上传完成时通知用户

**方案**:
- 后端事件总线：任务状态变更时触发通知事件
- 通知渠道：
  - 企业微信群机器人（Webhook URL）
  - Server酱 / PushPlus（推送到个人微信）
  - 页面内通知（WebSocket 推送 + 角标）
- 配置页面：让用户填 Webhook URL，选择通知渠道

---

### 四、任务管理器

**场景**: 多个训练任务并行，需要统一管理

**改进项**:
- 任务列表：显示运行中/排队中/已完成的任务
- 操作：暂停、恢复、终止、查看日志
- 资源占用：每个任务的 GPU/CPU/内存占用
- 任务队列：GPU 资源不足时排队等待

---

### 五、文件编辑器增强

**改进项**:
- 语法高亮支持更多语言（当前可能已有，确认覆盖范围）
- 代码自动补全（LSP 或简单关键字补全）
- 多文件搜索替换（跨文件 grep + 替换）
- Git 集成：显示修改状态、提交、推送
- Markdown 实时预览

---

### 六、安全加固

**改进项**:
- HTTPS 支持（自签证书或 Let's Encrypt）
- 登录失败锁定（连续失败 N 次锁定 IP）
- 操作审计日志（谁在什么时间做了什么）
- 文件访问权限细化（共享/私有目录）
- WebSocket 连接认证（当前终端 WS 无认证）
- CSRF 防护

---

### 七、系统信息仪表盘

**改进项**:
- 磁盘使用率（每个盘/分区）
- 网络流量实时监控（上下行速度）
- 进程列表（可按 CPU/内存排序，支持杀进程）
- 系统运行时间
- Windows 更新状态

---

### 八、远程桌面 / 屏幕串流

**方案**:
- noVNC（VNC over WebSocket）
- 或 RDP over WebSocket
- 实现在浏览器中查看/控制桌面

---

### 九、模型部署辅助

**场景**: 训练完的模型快速部署为 API

**改进项**:
- 一键启动 FastAPI/Gradio 推理服务
- 模型文件管理（版本、大小、指标）
- API 端点测试界面
- 自动端口分配

---

### 十、移动端适配优化

**改进项**:
- 左侧面板折叠为抽屉
- 底部 Tab 导航（文件/编辑/终端/监控）
- 手势操作（左滑返回、下拉刷新）
- 触屏友好的文件选择

---

### 十一、定时任务 / Cron

**场景**: 定时备份、定时训练、定时清理

**改进项**:
- 可视化 Cron 表达式编辑器
- 任务执行历史记录
- 失败重试策略
- 与通知系统联动（执行完通知）

---

### 十二、终端增强（进一步）

**改进项**:
- 多终端标签页（同时开多个终端）
- 终端会话持久化（断线重连恢复历史）
- 终端搜索（Ctrl+F 搜索输出内容）
- 终端录制/回放
- SSH 连接（连接远程 Linux 服务器）

---

## 优先级建议

| 优先级 | 功能 | 原因 |
|--------|------|------|
| P0 | 用户设置持久化 | 基础功能，影响日常使用 |
| P0 | WebSocket 终端认证 | 安全漏洞，任何人都能连 |
| P1 | 云盘 UI 完善 | 后端已有，前端补齐即可 |
| P1 | 通知系统 | 训练完成提醒是刚需 |
| P2 | 磁盘/网络监控 | 服务器运维基础 |
| P2 | 任务管理器 | 多任务并行时需要 |
| P3 | 文件编辑器增强 | 提升开发体验 |
| P3 | 安全加固 | 正式部署前必须做 |
| P4 | 远程桌面 | 锦上添花 |
| P4 | 模型部署 | 高级需求 |
